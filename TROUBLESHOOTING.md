# 누알피지 문제 해결

실제로 확인된 오류와 해결 방법, 설치 후 확인할 항목을 정리한 문서입니다. 비슷한 증상이라도 원인은 다를 수 있습니다.

## Windows에서 압축을 푼 뒤 설치 설정 오류가 나타나는 경우

확인된 사례에서는 Windows 보안 차단으로 `player-access.js`가 빠졌고, 다음 오류가 나타났습니다.

> 누알피지 설치 설정이 완료되지 않았습니다.  
> config.js의 Supabase URL과 공개 키를 확인한 뒤 새로고침해주세요.

1. 처음 내려받은 ZIP 파일을 마우스 오른쪽 버튼으로 누르고 **속성**을 엽니다.
2. **차단 해제** 항목이 있다면 체크하고 **적용**을 누릅니다.
3. 새 폴더에 압축을 다시 풉니다.
4. `config.js`, `player-access.js` 등 필요한 파일이 들어 있는지 확인하고 누알피지를 다시 실행합니다.

차단 해제가 없거나 오류가 계속되면, 파일 누락이 같은 원인인지 먼저 확인하세요.

## 새 방을 만든 직후 온라인 저장 오류가 나타나는 경우

> 온라인 저장 오류: 온라인 방을 찾지 못했습니다. 방 정보를 다시 확인해주세요.

확인된 사례에서는 사이트 파일과 Supabase의 저장 기능 버전이 서로 달랐습니다. 브라우저 Console에는 `jpp1e_save_room` 요청의 `HTTP 404`, `PGRST202` 오류가 나타났습니다. 이때 화면의 문구는 실제로 방이 삭제되었다는 뜻이 아니었습니다.

[UPDATE-GUIDE.ko.md](UPDATE-GUIDE.ko.md)에 따라 업데이트하세요. **2026-09-20 안정화판을 이미 적용했다면 [FOLLOWUP-UPDATE.ko.md](FOLLOWUP-UPDATE.ko.md)를 확인하세요.**

업데이트 후 시험 방을 만들어 저장하고, 다른 브라우저에서 PL로 입장해 메시지를 보냅니다. 새로고침과 재접속 후에도 내용이 남는지 확인하세요.

`PGRST203`, `42883`, `SERVER_RPC_MISSING`이 표시될 때도 사이트 파일과 Supabase 업데이트를 모두 마쳤는지 확인하세요. 오류 코드만으로 방이 없어진 것으로 판단하지 마세요.

## 설치 SQL 실행 중 realtime.topic() 오류가 나타나는 경우

확인된 이전 설치 SQL에서는 `realtime.topic()`이 없어 설치가 완료되지 않았습니다. 처음 설치 중이라면 이번 업데이트 파일의 `setup-helper.html`에 들어 있는 설치 SQL을 사용하세요. 기존 설치를 업데이트하는 경우에는 [UPDATE-GUIDE.ko.md](UPDATE-GUIDE.ko.md)를 따르세요.

## 이미지가 올라가지 않는 경우

1. Supabase에서 누알피지에 사용하는 프로젝트를 엽니다.
2. **Edge Functions**에 이름이 정확히 `jpp1e-assets`인 항목이 있는지 확인합니다.
3. 해당 항목이 없으면 `setup-helper.html`의 이미지 기능 설치 안내를 따릅니다. 이미 있다면 코드 적용을 마쳤는지 확인합니다.

`HTTP 401`이 표시되면 설치 마법사에 입력한 공개 키가 같은 프로젝트의 키인지, `jpp1e-assets`의 **Verify JWT with legacy secret**이 안내대로 꺼져 있는지 확인하세요.

`STORAGE_UPLOAD_FAILED`는 이미지 저장 단계에서 실패했다는 뜻입니다. 연결 오류만으로 원인을 단정하거나 Storage를 공개로 바꾸지 마세요. **방 코드 → 설치 연결 진단**에서 어느 단계가 실패하는지 확인합니다.

## 플레이어가 초대 코드로 들어갈 수 없는 경우

- GM과 PL이 **같은 누알피지 사이트 주소**를 열었는지 확인합니다. 각자 따로 설치한 사이트에서는 다른 설치본의 방에 들어갈 수 없습니다.
- 초대 코드가 이미 사용되었거나 GM이 회수한 코드인지 확인합니다.
- 전에 입장했던 플레이어는 처음 받은 초대 코드가 아니라 **재접속 코드**를 사용합니다.

## 한동안 사용하지 않은 뒤 연결되지 않는 경우

Supabase 무료 프로젝트는 사용량이 적으면 일시정지될 수 있습니다.

1. 설치자가 **Supabase Dashboard**에서 누알피지에 사용하는 프로젝트를 엽니다.
2. 일시정지 상태라면 화면의 **Resume project** 또는 **Restore** 안내에 따라 다시 활성화합니다.
3. 준비가 끝나면 누알피지를 새로고침하고 **방 코드 → 설치 연결 진단**을 다시 실행합니다.

누알피지에서 새로고침하는 것만으로는 일시정지가 풀리지 않습니다. 복구 가능 기간과 방법은 [Supabase 공식 안내](https://supabase.com/docs/guides/platform/free-project-pausing)와 프로젝트 화면을 확인하세요.

## 설치 연결 진단에서 시험 이미지 정리가 실패한 경우

진단은 작은 PNG 이미지를 올려 이미지 기능을 확인한 뒤 삭제를 시도합니다. 삭제가 실패하거나 업로드 결과를 받지 못하면 시험 이미지가 남을 수 있습니다.

직접 정리하려면 Supabase의 **Storage → jpp1e-assets**에서 파일을 확인하세요. 진단에서 만든 시험 파일임을 확실히 확인한 경우에만 그 파일을 삭제합니다. 구분할 수 없다면 그대로 두세요. 기존 이미지를 한꺼번에 삭제하지 마세요.

## 공개하면 안 되는 정보

오류 화면이나 내용을 공개할 때는 다음 정보가 포함되지 않도록 가리세요.

- Supabase의 `secret` 키, `service_role` 키
- VAPID private key, DB 비밀번호, 관리 키
- 초대 코드와 재접속 코드
- 요청 본문과 요청 헤더
- 서명된 이미지 주소(signed URL) 전체

설치에 사용하는 **공개 키(Publishable Key 또는 anon key)**와 비밀 키를 혼동하지 마세요.
