# 누알피지 문제 해결 기록

이 문서는 실제로 확인된 오류와 해결 방법을 기록합니다. 비슷해 보이는 증상이라도 원인이 같다고 단정하지 않습니다.

## AI 오류 진단 기본 규칙

1. 사용자가 제공한 실제 오류 메시지, 브라우저 Console, 현재 사용 중인 파일, 이 문서의 기록을 먼저 확인한다.
2. 파일명이나 버전명만으로 파일의 역할과 내용을 추정하지 않는다. 실제 파일 내용을 확인한 뒤 판단한다.
3. 비슷한 증상만으로 과거 오류와 같은 원인이라고 단정하지 않는다.
4. 원인이 확인되기 전에는 Supabase 프로젝트 재생성, 데이터베이스 초기화, 테이블 또는 데이터 삭제, RLS 비활성화, 키 재발급처럼 영향이 큰 조치를 우선 제안하지 않는다.
5. 정상 작동하는 경로와 실패하는 경로를 비교한다.
6. 사용자 화면의 친절한 오류 문구보다 서버 응답과 브라우저 Console의 실제 오류 코드와 응답 내용을 우선 확인한다.
7. 프런트엔드와 Supabase 데이터베이스 또는 Edge Function의 버전 불일치 가능성을 확인한다.
8. Supabase 함수 오류에서는 함수의 존재 여부뿐 아니라 함수명, 인자 이름, 인자 타입, 인자 개수, 반환 타입, 클라이언트가 실제로 보내는 인자를 함께 비교한다.
9. Publishable Key 또는 anon key와 secret 또는 service_role key를 구분한다. 진단을 위해 secret 또는 service_role key의 공개를 요구하지 않는다.
10. 수정하기 전에 실패 동작을 재현하고 어느 단계에서 실패하는지 확인한다.
11. 가능한 한 영향 범위가 작은 수정부터 적용한다.
12. 과거 해결책은 현재 버전과 조건이 같은지 확인한 뒤 적용한다.
13. 확인된 사실, 가능성이 높은 원인, 아직 확인되지 않은 가설을 구분해서 설명한다.
14. 수정 후에는 원래 실패하던 동작을 다시 실행하고 저장, 재접속, 새로고침 등 관련 회귀 동작도 확인한다.
15. 사용자가 개발자가 아니더라도 필요한 진단 절차를 생략하지 않는다. 대신 어떤 화면에서 무엇을 확인해야 하는지 구체적으로 설명한다.

## Windows 압축 해제 시 일부 JavaScript 파일 누락

발견 상태: 확인됨
해결 상태: 해결 완료

### 에러

Windows에서 내려받은 ZIP 파일을 압축 해제할 때 보안 경고가 나타나며 일부 JavaScript 파일이 압축 해제되지 않을 수 있습니다.

이 경우 누알피지를 실행했을 때 다음과 같은 설치 설정 오류가 나타날 수 있습니다.

누알피지 설치 설정이 완료되지 않았습니다.
config.js의 Supabase URL과 공개 키를 확인한 뒤 새로고침해주세요.

실제 확인 사례에서는 player-access.js가 Windows 보안에 의해 차단되었으며 같은 방식으로 다른 JavaScript 파일도 누락될 수 있었습니다.

### 원인

Windows가 인터넷에서 내려받은 ZIP 내부의 일부 JavaScript 파일에 보안 차단을 적용하면서 정상적으로 압축 해제되지 않은 것이 원인이었습니다.

### 어떤 것으로 고침

원본 ZIP 파일을 마우스 오른쪽 버튼으로 누른 뒤 속성을 열고 차단 해제를 적용했습니다.

그 뒤 기존에 압축을 풀었던 폴더를 사용하지 않고 새 폴더에 다시 압축을 풀었습니다.

config.js와 player-access.js를 포함한 배포 파일이 정상적으로 존재하는 것을 확인한 뒤 다시 실행했습니다.

### 확인 방법

누락되었던 JavaScript 파일이 압축 해제된 폴더에 존재하는지 확인합니다.

누알피지를 다시 실행했을 때 설치 설정 오류가 더 이상 나타나지 않는지 확인합니다.

## 새 방 생성 직후 온라인 저장 오류

발견 상태: 확인됨
해결 상태: 해결 완료

### 에러

새 GM 방을 생성한 직후 다음 오류가 나타났습니다.

온라인 저장 오류: 온라인 방을 찾지 못했습니다. 방 정보를 다시 확인해주세요.

브라우저 Console에서는 jpp1e_save_room 요청이 HTTP 404로 실패했으며 PostgREST 오류 코드 PGRST202가 확인되었습니다.

### 원인

당시 프런트엔드는 jpp1e_save_room을 호출하면서 다음 6개 인자를 전송하고 있었습니다.

p_room_code
p_owner_secret
p_name
p_rule
p_state
p_expected

그러나 기존 Supabase 프로젝트에는 p_expected가 없는 5인자 jpp1e_save_room만 존재했습니다.

프런트엔드와 Supabase 함수 시그니처가 서로 달라 PostgREST가 호출할 함수를 찾지 못한 것이 원인이었습니다.

사용자 화면의 온라인 방을 찾지 못했다는 문구는 실제 원인을 정확히 나타낸 것이 아니었습니다. 당시 클라이언트가 HTTP 404를 방 없음 오류로 변환하고 있었기 때문입니다.

### 어떤 것으로 고침

현재 설치 SQL에 포함된 jpp1e_save_room 정의를 기준으로 Supabase 함수를 갱신했습니다.

레거시 5인자 저장 함수는 구형 클라이언트 호출을 차단하도록 유지하고, p_expected를 포함한 최신 6인자 optimistic CAS 저장 함수를 추가했습니다.

두 함수에 필요한 execute 권한도 함께 적용했습니다.

### 확인 방법

Supabase SQL Editor에서 수정 SQL 실행 후 Success. No rows returned가 표시되는 것을 확인했습니다.

그 뒤 새로운 GM 방을 생성하여 온라인 저장이 정상적으로 이루어지는 것을 확인했습니다.

별도의 시크릿 브라우저 창에서 새로운 플레이어로 입장하여 채팅을 전송하고 새로고침한 뒤에도 정상 상태를 확인했습니다.

플레이어 재접속 코드로 다시 입장되는 것을 확인했습니다.

GM 측에서 캐릭터 기능치를 변경한 뒤 강력 새로고침하고 브라우저를 다시 실행하여 재진입해도 변경한 값이 유지되는 것을 확인했습니다.

브라우저 Console에서 기존 jpp1e_save_room HTTP 404와 PGRST202 오류가 더 이상 발생하지 않는 것을 확인했습니다.

## 기록 원칙

이 문서에는 실제 원인과 해결 방법이 확인된 오류를 우선 기록합니다.
확인되지 않은 추측성 원인은 확정 해결 사례처럼 추가하지 않습니다.
