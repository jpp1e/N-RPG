# 누알피지 업데이트 안내

이 문서는 기존에 누알피지를 설치해서 사용하고 있는 경우를 위한 안내입니다.

**2026-09-20 안정화판을 이미 적용했다면 이 업데이트를 다시 진행하지 말고 [FOLLOWUP-UPDATE.ko.md](FOLLOWUP-UPDATE.ko.md)를 확인하세요.**

처음 설치하는 경우에는 `INSTALL.txt`와 `setup-helper.html`을 사용하세요. `INSTALL.sql`은 **누알피지를 처음 설치할 때 사용하는 파일**입니다. 기존 설치를 업데이트할 때는 실행하지 마세요.

이 안내의 기준 설치본은 `jpp1e/N-RPG`의 커밋 `9046b2125a6ae7aeb3c2ada8e2c1afbef51ecb24`입니다. 다른 버전이거나 설치 SQL을 직접 수정한 경우에는 이 업데이트가 적용 대상인지 먼저 확인해야 합니다.

## 1. 업데이트를 준비합니다

참가자가 없는 시간에 진행하세요. 중요한 방은 누알피지의 백업 기능으로 보관하고, 기존에 Cloudflare에 올렸던 파일과 사용 중인 Project URL·공개 키도 준비합니다.

현재 사용 중인 Supabase 프로젝트와 Cloudflare Pages 프로젝트를 그대로 사용합니다.

## 2. Supabase를 업데이트합니다

1. Supabase에서 현재 누알피지에 사용하는 프로젝트를 열고 **SQL Editor**로 이동합니다.
2. 이번 업데이트 파일의 `STABILIZATION_UPDATE.sql`을 메모장 등으로 열어 내용을 전부 복사합니다.
3. SQL Editor에 붙여넣고 **Run**을 누릅니다.

오류 없이 완료되면 다음 단계로 이동합니다. `Unsupported function/helper/trigger` 오류가 나오면 현재 설치본이 적용 대상과 맞지 않을 수 있으므로 여기서 멈추세요. 다른 오류는 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)를 확인하세요.

## 3. 이미지 기능을 업데이트합니다

1. 같은 Supabase 프로젝트에서 **Edge Functions → jpp1e-assets**를 엽니다.
2. `readable-source` 폴더의 `EDGE_ASSETS_SOURCE.ts`를 메모장 등으로 열어 내용을 전부 복사합니다.
3. `jpp1e-assets`의 코드 편집 화면에서 기존 코드를 복사한 내용으로 바꾸고 **Deploy**를 눌러 적용합니다.

이름은 `jpp1e-assets`로 유지합니다. 기존의 **Verify JWT with legacy secret** 설정도 꺼진 상태로 유지하세요.

## 4. Cloudflare에 올릴 ZIP 파일을 만듭니다

1. 이번 업데이트 파일의 `setup-helper.html`을 Chrome 또는 Edge로 엽니다.
2. 현재 누알피지에서 사용하는 **Project URL과 공개 키**를 입력합니다.
3. **Cloudflare 업로드 ZIP 만들기**로 새 ZIP 파일을 만듭니다.

처음 설치할 때 진행했던 설정을 다시 할 필요는 없습니다.

## 5. Cloudflare에 새 파일을 올립니다

현재 누알피지가 올라가 있는 **같은 Cloudflare Pages 프로젝트**를 엽니다. 새 파일을 올리는 화면에서 **Production**을 선택하고, 방금 만든 ZIP 파일을 올립니다.

Supabase SQL 실행, 이미지 기능 업데이트, Cloudflare 파일 교체를 모두 마쳐야 전체 업데이트가 적용됩니다.

## 6. 연결과 플레이어 접속을 확인합니다

누알피지를 닫았다가 다시 열고 새로고침합니다. 시험 방에서 **방 코드 → 설치 연결 진단**을 실행해 정상으로 표시되는지 확인하세요.

다른 브라우저나 시크릿 창에서 **같은 누알피지 사이트 주소**를 열고 플레이어(PL)로 입장합니다.

- GM과 PL이 보낸 메시지와 시트 변경이 서로 보이는지 확인합니다.
- 새로고침한 뒤에도 내용이 남아 있고, 재접속 코드로 다시 들어갈 수 있는지 확인합니다.
- 이미지가 정상으로 보이는지, 중앙 맵을 숨기면 PL에게 중앙 맵만 사라지고 시트 이미지는 그대로 보이는지 확인합니다.

연결 오류나 진단 시험 이미지 정리 실패가 나타나면 [TROUBLESHOOTING.md](TROUBLESHOOTING.md)를 확인하세요.

## 함께 플레이하는 사람에게 안내하기

누알피지는 함께 플레이하는 사람 중 한 명만 설치하면 됩니다. 다른 참여자에게는 **설치한 누알피지 사이트 주소와 초대 코드**를 전달하세요. 처음 입장한 뒤에는 재접속 코드를 사용합니다.

## 파일 확인

설치 마법사에서 만드는 Cloudflare용 ZIP은 입력한 설정과 생성 시각에 따라 달라지므로 고정된 확인값이 없습니다.
